// WaterSteward Enterprise — Vite entry point.
//
// Alpine.js is bundled and started by Livewire 4 — do NOT import or start a
// second Alpine instance here (it conflicts with Flux UI internals).
// App-specific JavaScript belongs below (compiled via `npm run build`).
